#include "main.h"
/**
 * _abs - Entry point
 * Desc - computes the absolute value of an integer
 * @int: integer checked
 * Return: int
 */
int _abs(int)
{
	int (r >= 0);
		return (r);

	else
	{
		r *= -1;
		return (r);
	}
}
