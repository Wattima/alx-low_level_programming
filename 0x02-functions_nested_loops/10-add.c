#include "main.h"
/**
 * add - Entry point
 * Desc - adds two integers and returns the result
 * @i: first integer
 * @j: second integer
 * Return: Sum
 */
int add(int i, int j)
{
	int sum;

	sum = i + j;
	return (sum);
}
